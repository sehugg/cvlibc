#include "cv.h"
#include "cvu_graphics.h"

// Replaced by a macro.
/*void cvu_set_sprite_color(struct cvu_sprite *sprite, enum cv_color color)
{
	// Preserve the early clock bit.
	sprite->tag = (sprite->tag & 0x80) | color;
}*/
